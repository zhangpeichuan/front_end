import 'package:flutter/material.dart';

class HYUnknownContent extends StatelessWidget {
  const HYUnknownContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('未知错误'),
    );
  }
}
